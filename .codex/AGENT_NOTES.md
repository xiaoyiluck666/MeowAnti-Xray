# Agent Notes

- 2026-04-27：从 `E:\Code2026\fabricmod\meowconsole` 白名单复制出本地新项目 `E:\Code2026\fabricmod\MeowAnti-Xray`。
- 本次拆分先保留 `com.meowconsole` 包名，优先完成可构建的功能拆分；包名迁移可作为后续独立重构。
- 用户明确要求后续两个项目分线程维护；本项目只承接反矿透相关工作。
- 本地 Git 已初始化并提交首版：`596509b Initial Meow Anti-Xray split`。
- 2026-05-19 性能测试备注：NeoForge `neoforge/run/server.properties` 为本地压测开启了 `enable-rcon=true`、`rcon.password=meow-local-rcon-20260519`、`pause-when-empty-seconds=0`、`view-distance=12`、`simulation-distance=8`；`neoforge/run/mods` 放入了 `spark-1.10.172-neoforge.jar` 与当前 `meowantixray-neoforge-1.2.4.jar`。Windows 上 spark 自动使用 built-in Java profiler。真实玩家 bot 尝试失败：`minecraft-protocol` 无 `26.1.2` 数据。
- 2026-05-19 压测中发现并修复 NeoForge palette serialized size 反射兼容问题；新增 `/antixray stress <world> <radius> <passes>` 作为 OP-only synthetic rewrite 压测入口。一次压测数据：radius=8 passes=5 处理 1445 chunks，elapsed 12635.254ms，avgChunkMs 8.744，内部 rewriteAvgMs 2.932；radius=12 passes=3 处理 1875 chunks，elapsed 15443.298ms，avgChunkMs 8.236，累计 rewriteAvgMs 2.895，rewriteMaxMs 32.260。
- 2026-05-19 追加 bot/shim 结论：直接给 `mineflayer/minecraft-protocol` alias `26.1.2 -> 1.21.11` 不够，configuration 需补 `settings`/ping/keepalive，play 阶段 1.21.11 包结构和 26.1.2 不兼容。最终采用 `C:\Temp\meow-mp-2612\raw-runner-2612.js` 临时 raw TCP runner，按 NeoForge 26.1.2 源码抽取 play packet id：clientbound keep_alive=0x2c、level_chunk_with_light=0x2d、player_position=0x48；serverbound keep_alive=0x1c、position_look=0x1f、player_loaded=0x2c、chunk_batch_received=0x0b、tick_end=0x0d。测试后已停止 raw runner node 进程。
- 2026-05-19 Fabric Carpet 假人测试备注：`run/server.properties` 为本地测试开启 `enable-rcon=true`、`rcon.password=meow-local-rcon-20260519`、`pause-when-empty-seconds=0`、`online-mode=false`、`view-distance=12`、`simulation-distance=8`、`allow-flight=true`；`run/mods` 放入 `fabric-carpet-26.1-beta-1+v251217.jar`、`spark-1.10.172-fabric.jar`、`meowantixray-fabric-1.2.4.jar`。Fabric 启动失败点在 Carpet：`carpet.mixins.json:Player_scarpetEventsMixin` 注入 `Player#doInteract` 的旧签名缺少 `net.minecraft.world.phys.Vec3` 参数，说明该 Carpet beta 对 MC `26.1.2` 运行期不兼容。一次 `runServer` 失败后 Gradle 拉起了 NeoForge dev server，RCON 未成功握手，已确认 PID 属于当前项目后强制结束，端口已释放。
- 2026-05-19 网络假玩家工具备注：`tools/mc-2612-load-runner.js` 使用 Node 内置 `net`/`zlib`/`crypto`，不需要 npm install。早期 bug 是把 clientbound configuration `0x0e` 误当 keepalive，实际为 `select_known_packs`，修复为 serverbound `0x07` + 空列表后可进 play。直接发 `move_player_pos_rot` 会触发生存移动校验和 `moved wrongly`，保留但默认推荐 `--rcon-teleport` 跑图；RCON `/tp` 仍由真实网络玩家接收 chunk 包并回 teleport/chunk batch ack。低 Y 远距离传送可能窒息，推荐压测命令加 `--teleport-y 180`。
- 2026-05-20 压测排障备注：runner 需在 teleport ack 后发送 position/look 与 tick_end，才能持续触发服务端新区块发送；否则 chunk 数只停在初始进服批次。8 人直接传到 `y=180` 会被原版飞行检测踢出，已给 runner 加 `--gamemode spectator`，通过 RCON 先切 spectator 再传送。`Run0520D` 在无界异步队列版本下触发 OOM，crash 报告 `neoforge/run/crash-reports/crash-2026-05-20_00.30.37-server.txt`；修复后 `Run0520E/F` 均 8/8 在线完成 90s，0 errors。`async-queue-size=16` 会出现更明显 `Can't keep up` 峰值，这是同步 fallback 背压带来的取舍。
- 2026-06-01 Paper parity 校准：本地临时拉取 `C:\Temp\paper-main-parity`，Paper HEAD `10a73fe40f39d51e6a35e55154229bc9508a16d1`。Paper anti-xray 关键文件仍为 `paper-server/src/main/java/io/papermc/paper/antixray/ChunkPacketBlockControllerAntiXray.java`、`ChunkPacketInfoAntiXray.java`、`ChunkPacketInfo.java` 与 `WorldConfiguration.Anticheat.AntiXray`。配置字段无新增：enabled、engineMode、maxBlockHeight、updateRadius、lavaObscures、usePermission、hiddenBlocks、replacementBlocks；默认 hidden/replacement 与本项目 2026-05-19 对齐项一致。主要差距仍在实现层：Paper 通过 PalettedContainer preset values + chunk packet info 在原生序列化阶段记录 bits/palette/index/buffer 并直接改 packet buffer；本项目模组层需 snapshot section/palette/packet 后异步重写，成本和一致性均不可能完全等价。可补差异：Paper 的随机假块是每个 chunk packet 运行期 ThreadLocalRandom/xorshift，不绑定世界 seed；本项目当前按 world seed/section/position 稳定随机，行为更稳定但与 Paper 不同。Paper mode HIDE 会按维度/高度使用 stone/deepslate/netherrack/end_stone 预置值；本项目 mode 1 为 naturalReplacement，需进一步做 golden case 对齐。Paper 只在 old solid -> new transparent 且 y <= maxBlockHeight+updateRadius-1 时触发 blockChanged；本项目已做类似判断并额外维护 player mask/reveal 和 explosion resync，属于模组层补强。
