# Meow Anti-Xray Project Context

- 本目录是从 MeowConsole 拆分出的反矿透独立项目。
- 目标 mod id / 构建产物名：`meowantixray`。
- 当前 Java 包名：`com.meowantixray`。
- Fabric 入口：`src/fabric/java/com/meowantixray/fabric/MeowAntiXrayFabricMod.java`。
- NeoForge 入口：`neoforge/src/main/java/com/meowantixray/neoforge/MeowAntiXrayNeoForgeMod.java`。
- 反矿透配置文件：`config/meowantixray.yml`。
- 已移除控制台、睡眠加速、玩家消息、飞行保护、Velocity、MCDR 兼容等非反矿透源码；保留本项目独立的 Modrinth 更新检测提示。

## Thread Boundary

- 后续 `MeowAnti-Xray` 相关任务单独开线程接管本目录。
- 本项目只维护反矿透配置、混入、区块包重写、reveals、性能统计和 Fabric/NeoForge 反矿透对齐。
- 控制台彩色、多人生睡觉、玩家消息、飞行保护、Velocity、MCDR、更新检查等任务回到 `E:\Code2026\fabricmod\meowconsole`。

## Verification

- 2026-04-27：`.\gradlew.bat :neoforge:test --console=plain` 通过。
- 2026-04-27：`.\gradlew.bat test :neoforge:test --console=plain` 通过。
- 2026-04-27：`.\gradlew.bat buildAllLoaders --console=plain` 通过。
- 2026-04-27：1.2.4 发布准备已清理 NeoForge `-test` 后缀，并给 Fabric 产物名加入 `fabric` 标志；`.\gradlew.bat clean buildAllLoaders --console=plain` 通过，产物包含 `meowantixray-fabric-1.2.4.jar` 与 `meowantixray-neoforge-1.2.4.jar`。
- 2026-04-27：修复 Fabric 爆炸 mixin 动态选择，`.\gradlew.bat runServer --console=plain` 可初始化到 `Meow Anti-Xray initialized`，随后因测试目录未同意 EULA 正常停止。
- 2026-04-27：NeoForge 端已升级到 `26.1.2.30-beta` / Minecraft `26.1.2` / Java `25`；`.\gradlew.bat :neoforge:runServer --console=plain` 成功启动到 `Done` 与 `Meow Anti-Xray startup complete`，随后手动停止测试进程。
- 2026-04-27：补测真实 NeoForge installer 流程：`neoforge-26.1.2.30-beta-installer.jar --install-server` 成功；复制 `meowantixray-neoforge-1.2.4.jar` 到安装服 `mods/` 后用 `run.bat nogui` 成功启动到 `Done` 与 `Meow Anti-Xray startup complete`。
- 2026-04-27：清理 VS Code 问题面板中的 JDT null/dead-code 诊断后，`.\gradlew.bat test --console=plain` 通过。
- 2026-04-27：新增独立 Modrinth 更新检测提示并补全中英更新日志后，`.\gradlew.bat test --console=plain` 通过。
- 2026-05-01：修复 `MeowServerCommands` 的 VS Code/JDT null type safety 告警后，`.\gradlew.bat compileJava` 与 `.\gradlew.bat :neoforge:compileJava` 通过。
- 2026-05-19：完成当前项目完成度盘点；`.\gradlew.bat test :neoforge:test --console=plain` 通过。
- 2026-05-19：按 PaperMC/Paper `main` HEAD `5c917dababca4087b9df4d44b415d85125479830` 对齐反矿透差异：补 Paper 默认隐藏/替换块、`bypass-permission` 默认节点、EntityBlock 不作为 mode2/3 假块、Paper update-radius 邻居集合测试，并保持 Fabric/NeoForge 权限桥为可选反射 + OP fallback；`.\gradlew.bat test :neoforge:test --console=plain` 与 `.\gradlew.bat buildAllLoaders --console=plain` 通过。
- 2026-05-19：完成 NeoForge 本地 spark 性能测试；安装 `spark-1.10.172-neoforge.jar` 到 `neoforge/run/mods`，开启本地 RCON 后运行 synthetic anti-xray rewrite 压测命令。真实 bot 连接未完成，原因是 `mineflayer/minecraft-protocol` 暂不支持 Minecraft `26.1.2`。可用 spark 报告：区块加载/forceload `https://spark.lucko.me/ExDlvACxiK`，短 profile `https://spark.lucko.me/gWGugZKjEy`，反矿透 synthetic rewrite 压测 `https://spark.lucko.me/MXVTdbfqAp`。最终验证 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。
- 2026-05-19：追加完成 `minecraft-protocol` 26.1.2 本地 shim 探测：npm 最新 `minecraft-data@3.110.2` 已知道 protocol `775`，但缺 `data/pc/26.1.2` 完整数据；临时复制/修补 1.21.11 数据只能通过 configuration，play 包结构/ID 会错位。随后用 raw TCP 协议 runner 按 NeoForge 源码 `GameProtocols.java` 抽取 26.1.2 packet ID，成功模拟离线玩家登录、回 keepalive、回 chunk batch 并触发真实 chunk 下发。正式 spark 跑图窗口：`SparkRunner1` 约 7.6s 收到 691 chunks / 38 batches，报告 `https://spark.lucko.me/wy4A3SM0jx`；metadata 显示 TPS 约 20、MSPT last1m mean 4.51ms / p95 7.78ms / max 45.94ms。该 raw runner 是临时压测工具，不是完整 mineflayer 支持。
- 2026-05-19：尝试改用 Carpet 假人进行 Fabric 真实区块加载压测；已在 `run/mods` 放入 `fabric-carpet-26.1-beta-1+v251217.jar`、`spark-1.10.172-fabric.jar` 与当前 Fabric 产物，并同意本地 `run/eula.txt`。但 `.\gradlew.bat runServer --console=plain` 在服务端启动前因 Carpet mixin 崩溃：`Player_scarpetEventsMixin#doInteract` 描述符缺少 26.1.2 的 `Vec3` 参数。结论：当前可用 Carpet beta 虽声明 `minecraft >26.0`，但运行期未对齐 Minecraft `26.1.2`，本轮无法用 Carpet 假人得到有效 spark 报告；残留 NeoForge dev server 进程已按当前项目路径限定清理，25565/25575 已释放。
- 2026-05-19：新增仓库内网络假玩家压测工具 `tools/mc-2612-load-runner.js`，直接实现 Minecraft `26.1.2` / protocol `775` 必需子集，绕开 Carpet 与 `mineflayer/minecraft-protocol` 版本缺口。NeoForge dev server 实测 4 个假网络玩家 + RCON 跑图 91s：连接 4/4、play 4/4、teleports 124、position packets 79、chunks 5103、batches 272、errors 0、chunks/s 56.07；spark 报告 `https://spark.lucko.me/xjeRM3esQF`。`.\gradlew.bat :neoforge:runServer --console=plain` 正常停止后 `BUILD SUCCESSFUL`，25565/25575 已释放。
- 2026-05-20：根据 PaperMC/Paper `main` HEAD `5c917da` 复核 anti-xray 异步处理：Paper 在原生 chunk packet 序列化生命周期中记录 buffer/palette/index 并提交轻量 `ChunkPacketInfoAntiXray` 到服务端 executor；本项目作为 Fabric/NeoForge 模组层实现需要 clone section/packet 快照，不能直接等价照搬，因此新增异步 rewrite 背压。配置新增 `async-queue-size`，默认 16；进入异步前先按 `async-worker-threads + async-queue-size` 获取 permit，超过容量直接同步 fallback，避免无界排队导致 heap OOM。最终 `.\gradlew.bat test :neoforge:test --console=plain` 通过。
- 2026-05-20：完成 NeoForge 8 假网络玩家 RCON spectator 跑图压测。修复前 8 人压测触发 `OutOfMemoryError: Java heap space`，栈落在 `FakeOreService$SectionSnapshot.stateAt`，根因是无界异步队列堆积大量快照任务。修复后 `async-queue-size=64` 跑 91s：38037 chunks、417.92 chunks/s、0 errors，spark `https://spark.lucko.me/FyVDZTMgHb`，heap used 6314MB，TPS last1m 19.31，MSPT last1m mean 29.93ms / p95 229.12ms。收紧为 `async-queue-size=16` 后 91s：36233 chunks、398.06 chunks/s、0 errors，spark `https://spark.lucko.me/yFi0jlgOf4`，heap used 4840MB，TPS last1m 17.39，MSPT last1m mean 45.93ms / p95 309.05ms。结论：16 是更保守的防 OOM 默认，生产可按内存把队列调到 32/64 换吞吐。
- 2026-05-20：发布准备版本号按用户要求从语义化首版 `1.0.0` 开始，重写 `CHANGELOG.md` 为面向使用者的中英文标准更新日志，并优化 `MOD_INTRO_ZH.md` / `MOD_INTRO_EN.md`。新增 Gradle 任务 `releaseAllLoaders`，会构建 Fabric/NeoForge 并把两个主 jar 复制到统一目录 `build/release/`。执行 `.\gradlew.bat clean releaseAllLoaders --console=plain` 通过，产物为 `build/release/meowantixray-fabric-1.0.0.jar` 与 `build/release/meowantixray-neoforge-1.0.0.jar`，已检查内嵌 Fabric/NeoForge metadata 版本均为 `1.0.0`。
- 2026-05-20：按用户要求将更新日志按版本号归档到独立目录 `changelogs/`：`changelogs/1.0.0.zh-CN.md` 与 `changelogs/1.0.0.en-US.md`；根目录 `CHANGELOG.md` 仅保留版本索引链接。
- 2026-05-22：按 Modrinth 地址 `https://modrinth.com/project/meowanti-xray` 完善独立更新提示：服务端启动后异步检查 Modrinth compatible version，发现新版本时向在线管理员/OP 发送固定格式消息（当前版本 -> 新版本、简短更新内容、下载直链、版本页），管理员上线时也会补发当前新版本提示；`/antixray status` 玩家执行时复用可点击格式。`.\gradlew.bat test :neoforge:test --console=plain` 与 `.\gradlew.bat buildAllLoaders --console=plain` 通过。
- 2026-05-22：为真实联机测试启动本地双端 dev server。Fabric 监听 `127.0.0.1:25565`，RCON `25575`；NeoForge 监听 `127.0.0.1:25566`，RCON `25576`。为避免测试到旧 jar 或 Carpet 26.1.2 崩溃问题，已把 `run/mods` 与 `neoforge/run/mods` 中原有 jar 临时移动到各自的 `disabled-for-local-test/` 子目录；当前 dev server 均加载项目源码版本 `meowantixray 1.0.0`。
- 2026-05-22：完成 Fabric 真实联机 spark 跑图采样，Fabric 监听 `127.0.0.1:25565`，报告 `https://spark.lucko.me/btQdhJh1gH`。采样 2m01s / 2400 ticks / 1 player：TPS 稳定 20.00，MSPT median 3.31ms / p95 19.7ms / max 94.6ms，server thread 主要时间在 `MinecraftServer.waitUntilNextTick()` idle；`meowantixray` 在 mods 视图占 server thread 3.55%，轻量单人跑图下开销可接受。过程中修复 Fabric 登录时权限 lambda 反射 `IllegalAccessException`：`MinecraftCompat.invokeSingleArg` 与双参数 overload 调用前执行 `method.setAccessible(true)`；`.\gradlew.bat compileJava --console=plain` 通过。
- 2026-05-22：按用户要求关闭本地 Fabric / NeoForge 测试服，RCON 返回 `Stopping the server` 后 25565/25566/25575/25576 已释放；同时更新 `changelogs/1.0.0.zh-CN.md` 与 `changelogs/1.0.0.en-US.md`，发布说明更突出独立项目的性能优势，并明确 MeowConsole 新版本将不再维护内置反矿透，后续反矿透功能由 Meow Anti-Xray 承接。
- 2026-05-22：修复 `ModrinthUpdateChecker.literal` 处 VS Code/JDT null type safety 黄色告警：为 Fabric/NeoForge 编译都加入 `org.eclipse.jdt:org.eclipse.jdt.annotation` 的 `compileOnly` 依赖，并将 `requireText` 标注为 JDT `@NonNull String`，避免 suppression 引出新的 JDT 提示；`.\gradlew.bat compileJava :neoforge:compileJava --console=plain` 通过。
- 2026-05-22：接入用户提供的 Modrinth API token 完成项目线上维护和首版发布。项目 ID `8pl8obwY`，slug `meowanti-xray`，标题 `Meow Anti-Xray`，状态仍为 `draft` / `requested_status=approved` 等待 Modrinth 审核；已填写中英文项目正文、分类 `optimization`/`utility`、附加分类 `game-mechanics`/`management`、GitHub source/issues/wiki，客户端 `unsupported`、服务端 `required`。执行 `.\gradlew.bat clean releaseAllLoaders --console=plain` 通过，并上传版本 `1.0.0`（version id `misc3vI5`，release/listed/featured，MC `26.1.2`，loaders `fabric` + `neoforge`），文件为 `meowantixray-fabric-1.0.0.jar` 与 `meowantixray-neoforge-1.0.0.jar`。
- 2026-05-22：用户指出 Modrinth 合并版本展示会误导为单文件同时兼容 Fabric/NeoForge；本地已修正更新检测版本比较，忽略 SemVer build metadata（如 `1.0.0+fabric` / `1.0.0+neoforge`）并新增 `ModrinthUpdateCheckerTest`，`.\gradlew.bat test :neoforge:test releaseAllLoaders --console=plain` 通过。Modrinth 已改为两个独立版本：`1.0.0+fabric`（version id `yeuyLiT2`，只挂 `meowantixray-fabric-1.0.0.jar` 与 Fabric loader）和 `1.0.0+neoforge`（version id `8lfSaQK0`，只挂 `meowantixray-neoforge-1.0.0.jar` 与 NeoForge loader）；旧合并版本 `misc3vI5` 已删除。上传时最初遇到 Modrinth/S3 临时错误，改用禁用环境代理的 requests 重试后成功。
- 2026-05-22：修正 Modrinth 版本兼容范围：Fabric 与 NeoForge 两个线上版本均标记 Minecraft `26.1`、`26.1.1`、`26.1.2`。本地 NeoForge metadata 原先为 `[26.1.2,26.1.3)`，已放宽为 `[26.1,26.1.3)`，`.\gradlew.bat releaseAllLoaders --console=plain` 通过，并确认 NeoForge jar 内 `neoforge.mods.toml` 的 minecraft `versionRange = "[26.1,26.1.3)"`。已删除旧 NeoForge version id `8lfSaQK0` 并重传新 `1.0.0+neoforge`，新 version id `WHv5uBDD`；Fabric version id 仍为 `yeuyLiT2`。
- 2026-06-01：校准 Modrinth 公开状态：项目 `8pl8obwY` / slug `meowanti-xray` 已于 2026-05-31 审核通过，当前状态 `approved`，线上版本仍为 Fabric `yeuyLiT2` 与 NeoForge `WHv5uBDD`，公开页 `https://modrinth.com/mod/meowanti-xray`。
- 2026-06-01：启动 1.0.1 稳定维护准备：增强 `/antixray status` 输出 loader / mod version / Minecraft version；增强 `/antixray profile` 输出 async 容量、in-flight、available permits 与同步 fallback 计数，方便服主判断是否触发异步背压；忽略本地 `.playwright-cli/` 测试缓存。验证 `.\gradlew.bat test :neoforge:test --console=plain` 与 `.\gradlew.bat buildAllLoaders --console=plain` 通过。
- 2026-06-01：完成 1.0.1 发布准备：`mod_version` 已升到 `1.0.1`，新增 `changelogs/1.0.1.zh-CN.md` 与 `changelogs/1.0.1.en-US.md`，根目录 `CHANGELOG.md` 已加入 1.0.1 索引；再次验证 `.\gradlew.bat buildAllLoaders --console=plain` 通过。
- 2026-06-01：开始性能路径瘦身，目标是不改效果前提下减少热路径分配与重复复制。已把 `SectionSnapshot` 从全量 `states[]` 改为按需从 palette/unpacked 还原，去掉 `prepareChunkRewrite` 到 async task 的一层重复 clone，并为 `remapDensePaletteIndices` 保留兼容重载。验证 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。
- 2026-06-01：补充洞穴可见性回归测试：合成 section 验证连通到边界的洞穴空气旁隐藏矿石仍保持 exposed、不被回填；完全包裹矿石仍为 unexposed、可回填。验证 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。
- 2026-06-01：完成 1.0.1 发布到 Modrinth：本地执行 `.\gradlew.bat test :neoforge:test releaseAllLoaders --console=plain` 通过，产物为 `build/release/meowantixray-fabric-1.0.1.jar` 与 `build/release/meowantixray-neoforge-1.0.1.jar`。线上项目简介已更新为新版中英文表情版简介并包含压测验证表格；新增独立版本 `1.0.1+fabric`（version id `39vD2jaz`，文件 `meowantixray-fabric-1.0.1.jar`，依赖 Fabric API）和 `1.0.1+neoforge`（version id `gOiixdr9`，文件 `meowantixray-neoforge-1.0.1.jar`），均为 `listed` / `featured`，MC 兼容 `26.1`、`26.1.1`、`26.1.2`。
- 2026-06-02：启动 1.0.2 小维护：按 Paper `main` HEAD `10a73fe40f39d51e6a35e55154229bc9508a16d1` 修正 engine mode 1 / HIDE 自然替换规则，主世界按高度选择 `stone` / `deepslate`，不再因真实方块名包含 `deepslate` 而在 y>=0 替换为 deepslate；下界/末地仍为 `netherrack` / `end_stone`。新增 golden case 测试；`.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。
- 2026-06-02：补齐 Fabric 8 假网络玩家 RCON spectator 跑图压测时发现并修复 1.0.1 性能瘦身引入的 Fabric 崩溃：live `LevelChunkSection` local palette 读取误把 4096 个 block index 数当 palette size，触发 `MissingPaletteEntryException: Missing Palette entry for index 9`。修复为使用 `storageAccess.palette().getSize()`，新增 local palette 回归测试。修复后 Fabric dev server 8 个假网络玩家跑满 91s：connected 8/8、play 8/8、teleports 248、positionPackets 256、chunks 15833、batches 1844、errors 0、chunks/s 173.94。验证 `.\gradlew.bat test --console=plain` 与 `.\gradlew.bat buildAllLoaders --console=plain` 通过；压测结束后 25565/25566/25575/25576 已释放。
- 2026-06-02：完成 1.0.2 发布到 Modrinth：本地执行 `.\gradlew.bat clean test :neoforge:test releaseAllLoaders --console=plain` 通过，产物为 `build/release/meowantixray-fabric-1.0.2.jar` 与 `build/release/meowantixray-neoforge-1.0.2.jar`，已检查内嵌 Fabric/NeoForge metadata 版本均为 `1.0.2`。线上新增独立版本 `1.0.2+fabric`（version id `z8PGUT9X`，文件 `meowantixray-fabric-1.0.2.jar`，依赖 Fabric API）和 `1.0.2+neoforge`（version id `vmQINJlp`，文件 `meowantixray-neoforge-1.0.2.jar`），均为 `listed`，MC 兼容 `26.1`、`26.1.1`、`26.1.2`。
- 2026-06-02：完成 1.0.3 候选项 Paper parity 复查。Paper `main` 仍为 HEAD `10a73fe40f39d51e6a35e55154229bc9508a16d1`，anti-xray 配置字段、默认 hidden/replacement、engine mode、update radius 行为未新增变化。确认本项目额外默认隐藏 `ancient_debris`、`nether_quartz_ore`、`nether_gold_ore` 是独立反矿透模组的实用保护增强，不作为 parity bug 回退；已更新 `.codex/REFERENCE.md` / `.codex/ROADMAP.md` 并重命名相关测试方法避免误导。验证 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。
- 2026-06-06：按用户要求直接提升到 `1.1.0` 中版本并完成项目身份清理：Java 包名从 `com.meowconsole` 迁移到 `com.meowantixray`；核心类改为 `MeowAntiXrayMod`，命令类改为 `MeowAntiXrayCommands`，Fabric/NeoForge 入口与 mixin plugin 均改为 Meow Anti-Xray 命名；`mod_version=1.1.0`，新增中英文 `changelogs/1.1.0.*.md`。验证 `.\gradlew.bat clean test :neoforge:test releaseAllLoaders --console=plain` 通过，产物为 `build/release/meowantixray-fabric-1.1.0.jar` 与 `build/release/meowantixray-neoforge-1.1.0.jar`，已检查 jar metadata 版本与 Fabric entrypoint / NeoForge metadata 正确。随后补做启动测试：`.\gradlew.bat runServer --console=plain` 成功启动 Fabric 到 `Done` / `Meow Anti-Xray startup complete` / 更新检测 `1.1.0 is up to date for fabric`；`.\gradlew.bat :neoforge:runServer --console=plain` 成功启动 NeoForge 到 `Done` / `Meow Anti-Xray startup complete` / 更新检测 `1.1.0 is up to date for neoforge`；两端均用 RCON `stop` 正常停止，25565/25566/25575/25576 已释放。尚未上传 Modrinth。
- 2026-06-06：继续面向用户价值做 Paper parity / 性能诊断优化。已校准 PaperMC/Paper `main` HEAD `76d2ac758cb3abe75aceefa88207443768f585c6`，anti-xray 配置字段仍未新增；确认 Paper mode 2/3 会把 replacement blocks 纳入混淆目标，mode 1 仅隐藏 hidden blocks。本地修复 passes / target lookup：避免 mode 1 或关闭 replacement pass 时对 replacement blocks 做无效 blockChanged/reveal 候选，同时让 mode 3 对 replacement blocks 与 Paper 目标一致；复用静态 pass 数组，减少每个 chunk 重写时的小数组分配；补充目标集合回归测试。`/antixray profile` 新增 `pressure=normal/busy/saturated/disabled` 与 `syncFallbackRatio`，方便服主判断异步背压。验证 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。
- 2026-06-06：完成 1.1.0 全面启动与 spark 压测。Fabric：`runServer` 到 `Done` / `Meow Anti-Xray startup complete` / 更新检测正常；8 个 26.1.2 假网络玩家 RCON spectator 跑图 `91s`，8/8 在线，`28766` chunks，`316.01 chunks/s`，`0` errors，`/antixray profile` 显示 `chunks=28811`、rewrite avg `2.662ms`、max `33.198ms`、`syncFallbackRatio=43.0%`，spark `https://spark.lucko.me/A9EOkRRmFJ`。NeoForge：干净 `:neoforge:runServer` 到 `Done` / `Meow Anti-Xray startup complete` / 更新检测正常；8 假玩家跑图 `91s`，8/8 在线，`29699` chunks，`326.29 chunks/s`，`0` errors，profile `chunks=29723`、rewrite avg `2.893ms`、max `42.202ms`、`syncFallbackRatio=38.2%`，spark `https://spark.lucko.me/emnV8jJ4yG`。两端均已 RCON 停服，25565/25566/25575/25576 与项目 Java 进程已释放；中英文 1.1.0 更新日志已改为面向服主的发布说明；最终 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 与 `.\gradlew.bat releaseAllLoaders --console=plain` 通过，`build/release/` 已重新生成 `meowantixray-fabric-1.1.0.jar` 与 `meowantixray-neoforge-1.1.0.jar`。
- 2026-06-06：完成 1.1.0 发布到 Modrinth。线上新增独立版本 `1.1.0+fabric`（version id `RBQd8pvr`，文件 `meowantixray-fabric-1.1.0.jar`，依赖 Fabric API）和 `1.1.0+neoforge`（version id `k257upBH`，文件 `meowantixray-neoforge-1.1.0.jar`），均为 `listed`，MC 兼容 `26.1`、`26.1.1`、`26.1.2`。上传 token 只在本轮进程内使用，未写入 repo 或 `.codex`。
- 2026-06-06：新增本地 Modrinth 发布 helper `tools/publish-modrinth.ps1`。默认 dry-run 会执行 `releaseAllLoaders`、检查 Fabric/NeoForge jar metadata、检查线上重复版本并输出发布计划；传 `-Upload` 才读取 `MODRINTH_TOKEN` / `MODRINTH_API_TOKEN` 调用 Modrinth API 上传，脚本会拒绝重复发布 `<version>+fabric` / `<version>+neoforge`。已验证 `.\tools\publish-modrinth.ps1 -SkipBuild` 与 `.\tools\publish-modrinth.ps1` 均能识别当前 `1.1.0+fabric` / `1.1.0+neoforge` 已存在并阻止重复上传。
- 2026-06-06：处理 GitHub issue #1 `Server error with Polymer mod`。根因是 Fabric 下 `PlayerChunkSenderMixin` 在 HEAD 取消原版 `sendChunk` 并自行构造 `ClientboundLevelChunkWithLightPacket`，Polymer 需要 chunk packet 在自己的 packet context 内创建，因此报 `Some mod tried to create a chunk update packet, without the packet context present`。修复为检测到 Fabric + Polymer（`polymer-core` / `polymer-bundled` / `polymer`）时不取消原版 `sendChunk`，改在原版 `ServerGamePacketListenerImpl.send(Packet)` 前接管已创建的 `ClientboundLevelChunkWithLightPacket` 并走现有 rewrite；非 Polymer 环境保留原 HEAD 路径以维持现有异步性能。验证 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过；普通 Fabric 1 假客户端 13s 收到 `1682` chunks、0 errors，profile `rewriteTasks=1682`、rewrite avg `2.755ms`；带 Polymer `0.16.5+26.1.2` 可启动且未出现 issue 中的 Polymer missing-context 报错，但当前 raw runner 未完成 Polymer 额外握手，未进入 play。
- 2026-06-06：完成 issue #1 修复版 `1.1.1` 发布。真实客户端 `xiaoyiluck` 在 Fabric + Polymer bundled `0.16.5+26.1.2` 测试服进服、切换游戏模式并加载区块后，日志未再出现 Polymer missing context / `IllegalStateException`；`/antixray profile` 显示真实客户端触发 `chunks=2778`、`rewriteTasks=2778`、rewrite avg `2.907ms`、`syncFallbackRatio=6.6%`。本地 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过，`.\tools\publish-modrinth.ps1` dry-run 通过；线上新增 `1.1.1+fabric`（version id `u02Xc7ol`，文件 `meowantixray-fabric-1.1.1.jar`，依赖 Fabric API）和 `1.1.1+neoforge`（version id `bJBqlAOa`，文件 `meowantixray-neoforge-1.1.1.jar`），均为 `listed`，MC 兼容 `26.1`、`26.1.1`、`26.1.2`；已在 GitHub issue #1 评论发布链接 `https://github.com/xiaoyiluck666/MeowAnti-Xray/issues/1#issuecomment-4638657451`。
- 2026-06-08：收尾推送前用 JDK 25.0.2 执行 `.\gradlew.bat buildAllLoaders --console=plain` 通过；随后准备将本地 `main` 推送到 GitHub 并关闭已发布修复的 issue #1。
- 2026-06-08：完成 post-1.1.1 开发维护，不作为用户侧发版：未修改 `mod_version`，未新增用户 changelog。补充 `ModrinthUpdateCheckerTest`，明确 `1.1.1+fabric` / `1.1.1+neoforge` 与本地 `1.1.1` 比较相等；增强 `tools/publish-modrinth.ps1` dry-run/发布输出，增加 jar SHA512/SHA1、文件大小、重复线上版本详情和发布后 `.codex` 记录片段；`.codex/ROADMAP.md` 已把原 “Next Patch Candidates” 清理为 “Post-1.1.1 Developer Maintenance”。
- 2026-06-08：启动 `1.2.0` 候选开发，但尚未提升 `mod_version`。第一项用户侧改动为配置兼容增强：`FakeOreConfig` 轻量 YAML 解析现在支持带引号的 boolean/int/string 标量、带引号的内联列表项、带引号的缩进列表项、带引号的维度别名（如 `"nether"`），并且不会把引号内的 `#` 当作注释截断。新增 `FakeOreConfigTest.loadOrCreateAcceptsQuotedYamlScalarsAndInlineLists`；已验证 `.\gradlew.bat test --tests com.meowantixray.antixray.FakeOreConfigTest --console=plain` 与 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。
- 2026-06-08：继续完成 `1.2.0` 候选配置文档与升级体验：新生成的 `config/meowantixray.yml` 头部说明旧配置会补缺字段且支持带引号 YAML / 内联列表；README、中文 Modrinth 简介、英文 Modrinth 简介均新增 per-dimension 配置示例和旧配置兼容说明。补充生成配置头注释测试断言；`.\gradlew.bat test --tests com.meowantixray.antixray.FakeOreConfigTest --console=plain` 通过。仍未提升 `mod_version`。
- 2026-06-08：完成 `1.2.0` 候选真实 loader 配置 reload 冒烟。Fabric dev server 临时改用 `server-port=25567` / `rcon.port=25577` 启动成功，`/antixray status` 返回 `loader=fabric`、配置来源 `.\config\meowantixray.yml`、更新检测 `1.1.1 is up to date for fabric / MC 26.1.2`，`/antixray reload` 返回配置重载摘要，随后 RCON `stop` 正常停服并恢复 `run/server.properties` 原端口。NeoForge 发现已有本项目 dev server 运行在 `25566` / `25576`，直接对该实例执行 `/antixray status` 与 `/antixray reload`，返回 `loader=neoforge`、配置来源 `neoforge\run\config\meowantixray.yml`、更新检测 `1.1.1 is up to date for neoforge / MC 26.1.2`，随后 RCON `stop` 正常停服；25566/25576/25567/25577/25568/25578 均已释放。
- 2026-06-08：进入 `1.2.0` 发布准备：`gradle.properties` 已提升 `mod_version=1.2.0`，根 `CHANGELOG.md` 已加入 1.2.0 索引，新增 `changelogs/1.2.0.zh-CN.md` 与 `changelogs/1.2.0.en-US.md`，内容聚焦配置兼容、生成配置说明、README/Modrinth 配置示例和 Fabric/NeoForge reload 冒烟验证。
- 2026-06-08：`1.2.0` 发布前验证通过：使用 JDK 25.0.2 执行 `.\gradlew.bat clean test :neoforge:test releaseAllLoaders --console=plain` 成功，产物为 `build/release/meowantixray-fabric-1.2.0.jar` 与 `build/release/meowantixray-neoforge-1.2.0.jar`。执行 `.\tools\publish-modrinth.ps1 -SkipBuild` dry-run 成功，确认 jar metadata 版本均为 `1.2.0`，目标 Modrinth 版本为 `1.2.0+fabric` / `1.2.0+neoforge` 且未重复；Fabric SHA512 `1e581bfd5a2d9fe7ab459c557e33afae25934c6647fba0c5e9bae166719b54bf41e5c6c7fad20f8c394432a8b10ba9bf0f71853af73433c02138a920490b775b`，NeoForge SHA512 `cfb08f39ace56bf278ff608157d68f5fa8e7a4038fa46616568a18e34272e2de66f1fb8a238ec4e02a0bb1f36b3c203471e03c9db0175a45ea7747b8ed02ff9a`。尚未上传 Modrinth。
- 2026-06-08：完成 `1.2.0` 发布到 Modrinth。执行 `.\tools\publish-modrinth.ps1 -Upload` 成功，线上新增独立版本 `1.2.0+fabric`（version id `ZYvAlFb3`，文件 `meowantixray-fabric-1.2.0.jar`，依赖 Fabric API）和 `1.2.0+neoforge`（version id `8nPHUeNS`，文件 `meowantixray-neoforge-1.2.0.jar`），均为 `listed`，MC 兼容 `26.1`、`26.1.1`、`26.1.2`。上传 token 仅用于本轮进程环境变量，未写入 repo、`.codex` 或 Serena memory。
- 2026-06-09：启动 `1.2.1` 收口候选，先做低风险配置兼容补强：`FakeOreConfig` 现在把显式空内联列表 `[]` 视为用户配置，而不再误回退默认 hidden/replacement blocks；同时补充异常内联列表回退与引号内 `#` 保留测试。验证 `.\gradlew.bat test --tests com.meowantixray.antixray.FakeOreConfigTest --console=plain` 与 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 在 JDK `25.0.2` 下通过。未提升 `mod_version`，尚未新增 changelog 或发布产物。
- 2026-06-09：继续扩充 `1.2.1` 候选到用户可感知功能包。`/antixray status` 现在除 runtime / update 外，还会输出全局启用状态、engine mode、max-block-height、async 配置、配置文件路径，以及每个维度的 enabled/mode/max/hidden/replacement 计数；`/antixray reload` 改为返回 reload 后状态摘要和关键配置变更 diff；原 `/antixray debug` 诊断增强为多行 inspect 输出，并新增正式别名 `/antixray inspect`，可显示目标方块的 real/fake、是否 hidden/replacement/target、是否超出高度范围、是否 exposed、是否应 reveal、权限与 update-radius 等。补充 `FakeOreServiceTest` 覆盖 status/reload diff/inspect reason，并验证 `.\gradlew.bat test --tests com.meowantixray.antixray.FakeOreServiceTest --console=plain` 与 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 在 JDK `25.0.2` 下通过。仍未提升 `mod_version`，尚未新增 changelog 或发布产物。
- 2026-06-09：完成本地双 loader 命令冒烟。对 Fabric `127.0.0.1:25565` / RCON `25575` 和 NeoForge `127.0.0.1:25566` / RCON `25576` 分别执行 `antixray status`、`antixray reload`、`antixray inspect overworld 0 64 0` 与 `stop`。两端 `status` / `reload` 都正常返回扩展后的全局状态、async 容量、配置路径和 per-dimension 计数，`reload` 返回 `changes: no effective runtime changes`；`inspect` 也成功输出多行诊断。Fabric 当前配置路径为 `.\config\meowantixray.yml`，计数为 overworld `hidden=26/replacement=3`、nether `3/3`、end `4/1`；NeoForge 当前配置路径为 `neoforge\run\config\meowantixray.yml`，计数为 overworld `19/6`、nether `3/3`、end `4/1`。两端随后均正常 `Stopping the server`，确认 `25565/25566/25575/25576` 已释放。
- 2026-06-09：完成 `1.2.1` 发布准备并上传到 Modrinth。`gradle.properties` 已提升 `mod_version=1.2.1`，根 `CHANGELOG.md` 已加入 1.2.1 索引，新增 `changelogs/1.2.1.zh-CN.md` 与 `changelogs/1.2.1.en-US.md`，README、中文 Modrinth 简介、英文 Modrinth 简介均已同步这次的 status/reload/inspect 诊断增强与显式空列表配置兼容。使用 JDK `25.0.2` 执行 `.\gradlew.bat clean test :neoforge:test releaseAllLoaders --console=plain` 成功，产物为 `build/release/meowantixray-fabric-1.2.1.jar` 与 `build/release/meowantixray-neoforge-1.2.1.jar`。执行 `.\tools\publish-modrinth.ps1 -SkipBuild` dry-run 成功；Fabric SHA512 `a49cd0832336265f167f05e5374cfd82efa40e147a185ead6e8ebc698e1d7dfd0962c3273fc34a469353bf74114e67a89a6ec74db056965da552527c59db95c8`，NeoForge SHA512 `62b3f59c1920d51aed7ed2c25c526c2085d6eef899ece44171a15943a7c49b298203fb6f1e7f78c68afcebdd93845c94bb4f38ed50a04dc2a6b59494d485705d`。随后执行 `.\tools\publish-modrinth.ps1 -SkipBuild -Upload` 成功，线上新增独立版本 `1.2.1+fabric`（version id `lRWSPdkr`，文件 `meowantixray-fabric-1.2.1.jar`，依赖 Fabric API）和 `1.2.1+neoforge`（version id `Mh7pIebp`，文件 `meowantixray-neoforge-1.2.1.jar`），均为 `listed`，MC 兼容 `26.1`、`26.1.1`、`26.1.2`。上传 token 仅用于本轮进程环境变量，未写入 repo、`.codex` 或 Serena memory。
- 2026-06-09：完成 `1.2.2` 补丁收口，专门修正 `1.2.1` 发版后暴露的命令输出细节。`/antixray inspect` 现在稳定输出标准维度 ID（如 `minecraft:overworld`），不再暴露 `ResourceKey[...]`；`/antixray status`、`/antixray reload`、`/antixray inspect` 在控制台与 RCON 下改为更紧凑的单行输出，游戏内仍保持多行显示。新增 `MeowAntiXrayCommandsTest` 与 `FakeOreServiceTest.normalizeDimensionKeyStripsResourceKeyWrapper` 回归测试；使用 JDK `25.0.2` 执行 `.\gradlew.bat test --tests com.meowantixray.MeowAntiXrayCommandsTest --tests com.meowantixray.antixray.FakeOreServiceTest --console=plain` 通过，随后执行 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。`gradle.properties` 已提升 `mod_version=1.2.2`，根 `CHANGELOG.md` 已加入 1.2.2 索引，新增 `changelogs/1.2.2.zh-CN.md` 与 `changelogs/1.2.2.en-US.md`，README 与中英文项目简介已同步这次命令输出改进。尚未上传 Modrinth。
- 2026-06-09：补做 `1.2.2` 真实命令冒烟与全量验证。使用 JDK `25.0.2` 执行 `.\gradlew.bat clean test :neoforge:test buildAllLoaders --console=plain` 再次通过。Fabric dev server 真实启动到 `Done`，RCON 执行 `antixray status` / `antixray reload` / `stop` 正常；NeoForge dev server 真实启动到 `Done`，RCON 执行 `antixray status` / `antixray inspect overworld 0 64 0` / `antixray reload` / `stop` 全部正常，原始回包已确认 `status` 会带 runtime + update 信息，`inspect` 维度名输出为 `minecraft:overworld`，`reload` 返回单行紧凑摘要。未发现新的功能性阻塞问题。NeoForge 启动日志中的 `meowantixray (version 1.2.0 -> 1.2.2)` 是已有世界/环境版本差异提示，不影响本轮功能验证与发版。
- 2026-06-09：完成 `1.2.2` 发布到 Modrinth。执行 `.\tools\publish-modrinth.ps1` dry-run 成功，确认 `meowantixray-fabric-1.2.2.jar` 与 `meowantixray-neoforge-1.2.2.jar` metadata 版本均为 `1.2.2`，且线上不存在重复版本；Fabric SHA512 `39d6473caa32b69ff3c2a315d73e434dd22696c9f53781ab79571361e01246874c7f78623fbbe7f933f83bd401ced746321df288333bfef3b197250c57a33301`，NeoForge SHA512 `14af958308103b8d86bd79967090bc84bcc3e4283aaef47e0c2e2bb10d07de063659e4f299b47d31085548f1aa93951a9293e060096c4595001b3bd450ffa4fd`。随后执行 `.\tools\publish-modrinth.ps1 -SkipBuild -Upload` 成功，线上新增独立版本 `1.2.2+fabric`（version id `Nvxt3UCV`，文件 `meowantixray-fabric-1.2.2.jar`，依赖 Fabric API）和 `1.2.2+neoforge`（version id `lzz6pViR`，文件 `meowantixray-neoforge-1.2.2.jar`），均为 `listed`，MC 兼容 `26.1`、`26.1.1`、`26.1.2`。
- 2026-06-09：同步更新 Modrinth 项目简介。线上项目短描述已改为 `Paper-style server-side anti-xray for Fabric and NeoForge. No client install, per-dimension config, inspect/reload diagnostics.`；项目正文已按本地 `MOD_INTRO_EN.md` / `MOD_INTRO_ZH.md` 重新推送，补齐 `/antixray inspect`、更清晰的 `status/reload/inspect` 诊断说明、标准维度 ID 展示和显式空列表配置兼容说明。此次为项目页文案同步，未涉及新构建或新版本上传。
- 2026-06-10：完成 1.2.2 发布后仓库收口与下一步计划校准。PaperMC/Paper 远端 `main` 仍为 `76d2ac758cb3abe75aceefa88207443768f585c6`，与 2026-06-06 parity 基准一致，没有新的 anti-xray 上游变化需要跟进；`.codex/ROADMAP.md` 已校正到最新线上 `1.2.2` 并记录下一轮候选为平台抽象清理、双 loader 8 假玩家 profiling / async queue 调参建议、只读配置 UX。新增 `.gitignore` 规则忽略本地 `*-rcon-smoke-*.json` 冒烟输出。验证 `.\gradlew.bat test --tests com.meowantixray.MeowAntiXrayCommandsTest --tests com.meowantixray.antixray.FakeOreConfigTest --tests com.meowantixray.antixray.FakeOreServiceTest --console=plain` 在 JDK `25.0.2` 下通过。
- 2026-06-10：启动 1.3.0 候选维护，不提升 `mod_version`。完成第一段平台抽象清理：新增共享 `PlatformSupport`，集中 `LoadedModInfo` 排序与权限请求空值校验，Fabric/NeoForge 平台适配只保留 loader API 差异；补充 `PlatformSupportTest`。验证 `.\gradlew.bat test --tests com.meowantixray.platform.PlatformSupportTest --console=plain` 与 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。随后完成默认 `async-queue-size=16` 的 8 假网络玩家 profiling：Fabric 临时端口 `25567/25577`，91s、34408 chunks、378.04 chunks/s、0 errors，profile rewrite avg `2.607ms` / max `33.698ms` / `syncFallbackRatio=42.3%`，spark `https://spark.lucko.me/5E7fw7pAwG`；NeoForge `25566/25576`，91s、32547 chunks、357.55 chunks/s、0 errors，profile rewrite avg `2.845ms` / max `40.738ms` / `syncFallbackRatio=38.6%`，spark `https://spark.lucko.me/3gRMdXzJoW`。结论：默认队列 16 仍优先推荐内存安全，当前不需要把 README/Modrinth 改成默认建议 32/64；内存充足服主继续按文档自行调大并观察 `/antixray profile`。测试后 Fabric 端口已恢复 `25565/25575`，Fabric/NeoForge 当前项目测试服均已停止，`25566/25567/25576/25577` 已释放。
- 2026-06-27：完成 Minecraft `26.2` 兼容升级初验，暂不提升 `mod_version` 且未上传 Modrinth。官方校准：Mojang manifest 显示 `26.2` 为 2026-06-16 release 且 Java 仍为 25；Fabric 官方 metadata 可用 `fabric-loader 0.19.3` / `fabric-api 0.153.0+26.2`；NeoForge 官方 Maven 可用 `26.2.0.7-beta`。本地已将构建目标升级到 Minecraft `26.2`、Fabric Loader `0.19.3`、Fabric API `0.153.0+26.2`、NeoForge `26.2.0.7-beta`，Fabric metadata 限制为 `>=26.2 <26.3`，NeoForge metadata 限制为 `[26.2,26.3)`，发布脚本默认 game_versions 改为 `26.2`。验证：`.\gradlew.bat compileJava :neoforge:compileJava --console=plain` 通过；`.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过；检查 jar 内 Fabric/NeoForge metadata 均已展开到 26.2 依赖范围；本地 `runServer` 聚合启动 Fabric `25565/25575` 与 NeoForge `25566/25576` 均到 `Done` / `Meow Anti-Xray startup complete`，NeoForge RCON `antixray status` 返回 `loader=neoforge, version=1.2.2, minecraft=26.2`，随后 RCON `stop` 正常停服，`25565/25566/25575/25576` 与当前项目 Java 进程已释放。注意：旧 `neoforge/run/world` 自动升级到 26.2，并出现已有环境版本差异提示 `neoforge (version 26.1.2.30-beta -> 26.2.0.7-beta)`；这是本地测试世界状态，不是仓库配置。
- 2026-06-27：完成 `1.3.0` 版本收口，支持 Minecraft `26.2` 并完成双 loader 压测，尚未上传 Modrinth。`mod_version=1.3.0`；`tools/mc-2612-load-runner.js` 默认升级到 `--mc-version 26.2` / protocol `776`，保留 `26.1.2` / protocol `775` profile，并修复 26.2 configuration custom payload 原样回发导致服务端解码失败的问题。压测报告新增 `reports/1.3.0-minecraft-26.2-pressure-report.md`：Fabric 8 假网络玩家 92s、32893 chunks、357.49 chunks/s、0 errors，profile rewrite avg 2.839ms / max 34.980ms / syncFallbackRatio 42.0%；NeoForge 92s、35593 chunks、386.86 chunks/s、0 errors，profile rewrite avg 2.719ms / max 46.369ms / syncFallbackRatio 39.0%。`/antixray status` 均确认 `version=1.3.0`、`minecraft=26.2`。PaperMC/Paper 远端 `main` 仍为 `76d2ac758cb3abe75aceefa88207443768f585c6`，与 2026-06-06/10 parity 基线一致，没有新的 anti-xray 上游变化需要跟进。已新增中英文 `changelogs/1.3.0.*.md`，更新 `CHANGELOG.md`、README、中英文 Modrinth 简介与 `.codex/REFERENCE.md`。验证：`node --check tools\mc-2612-load-runner.js` 通过；`.\gradlew.bat clean test :neoforge:test releaseAllLoaders --console=plain` 通过；release jar metadata 检查通过，产物为 `build/release/meowantixray-fabric-1.3.0.jar` 与 `build/release/meowantixray-neoforge-1.3.0.jar`。
- 2026-06-27：修复 VS Code / JDT 空值告警。`FakeOreService#getStatusSnapshot` 中把 `keys.sort(String::compareToIgnoreCase)` 改为显式 lambda，避免 JDT 把方法引用误判为 nullable receiver；`PlatformSupport.hasUsablePermissionRequest` 明确标注 `@Nullable ServerPlayer` 与 `@Nullable String` 参数；`LoadedModInfo.originPath` 明确标注 `@Nullable Path`，与测试里传入 `null` 的语义保持一致。验证：`.\gradlew.bat compileJava compileTestJava --console=plain` 与 `.\gradlew.bat test --tests com.meowantixray.platform.PlatformSupportTest --console=plain` 通过。
- 2026-06-28：继续收口 VS Code / JDT null type annotation 告警。`PlatformSupport.sortedLoadedMods` 改为 `<T extends LoadedModInfo> List<T> sortedLoadedMods(List<T> mods)`，避免把 `List<@NonNull LoadedModInfo>` 强制转成 `Collection<...>` 或更弱注解的返回类型；`MeowAntiXrayCommands` 的 `String::strip` 与测试里的 `LoadedModInfo::id` 改为显式 lambda；`LoadedModInfo` 新增不带 `originPath` 的便捷构造器，测试不再直接传裸 `null`。验证：`.\gradlew.bat compileJava compileTestJava --console=plain` 与 `.\gradlew.bat test --tests com.meowantixray.platform.PlatformSupportTest --console=plain` 通过。
- 2026-06-28：修正上一条 JDT 告警收口方式。`LoadedModInfo` 是 record/final，不能用 `<T extends LoadedModInfo>` 作为长期签名；真正根因是 `loadedMods` / `sortedLoadedMods` / loader 实现 / 测试之间的 type-use null annotation 契约不一致。已给 `ModPlatform.loadedMods()`、Fabric/NeoForge `loadedMods()`、`PlatformSupport.sortedLoadedMods()` 和测试局部变量统一标注 `Collection/List<@NonNull LoadedModInfo>`，并为 root 与 NeoForge 测试源码补 `testCompileOnly org.eclipse.jdt.annotation`。验证：`.\gradlew.bat compileJava compileTestJava --console=plain` 与 `.\gradlew.bat test --tests com.meowantixray.platform.PlatformSupportTest --console=plain` 通过。
- 2026-06-28：完成 `1.3.0` 发布到 Modrinth，并同步项目主页简介。发布前验证 `.\gradlew.bat clean test :neoforge:test releaseAllLoaders --console=plain` 通过，`node --check tools\mc-2612-load-runner.js` 通过；release jar metadata 确认 Fabric `version=1.3.0`、`minecraft >=26.2 <26.3`、`fabricloader >=0.19.3`、`fabric-api >=0.153.0+26.2`，NeoForge `version=1.3.0`、`minecraft [26.2,26.3)`、`neoforge [26.2.0.7-beta,)`。`.\tools\publish-modrinth.ps1 -SkipBuild` dry-run 通过，随后 `.\tools\publish-modrinth.ps1 -SkipBuild -Upload` 成功；线上新增独立版本 `1.3.0+fabric`（version id `rj3Ex0cy`，文件 `meowantixray-fabric-1.3.0.jar`，依赖 Fabric API）和 `1.3.0+neoforge`（version id `OquEDb5X`，文件 `meowantixray-neoforge-1.3.0.jar`），均为 `listed`，MC 兼容 `26.2`。Modrinth 项目短描述已同步为支持 Minecraft 26.2，项目正文已按本地 `MOD_INTRO_EN.md` / `MOD_INTRO_ZH.md` 推送，API 复查 `body_has_26_2=True`、`body_has_1_3_0=True`。
- 2026-06-28：按长期支持策略建立版本分支隔离。当前 `main` 保持 Minecraft `26.2` / `1.3.x+` 主线；已从 `e7b3966`（`1.2.2` 发布提交）切出并推送 `maintenance/26.1.x`，专门维护 Minecraft `26.1`、`26.1.1`、`26.1.2` / `1.2.x` 补丁。Paper 官方 `main` 复查仍为 `76d2ac758cb3abe75aceefa88207443768f585c6`，无新的 anti-xray main 线变化；新增 `reports/paper-parity-status.md`，并在 `.codex/REFERENCE.md` 记录跨分支 cherry-pick 规则，避免 26.2 构建配置误合入 26.1.x。
