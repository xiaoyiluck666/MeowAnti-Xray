# Meow Anti-Xray Project Context

- 本目录是从 MeowConsole 拆分出的反矿透独立项目。
- 目标 mod id / 构建产物名：`meowantixray`。
- 当前仍保留 Java 包名 `com.meowconsole`，用于降低拆分时对 mixin、测试与跨 loader 代码的震动。
- Fabric 入口：`src/fabric/java/com/meowconsole/fabric/MeowConsoleFabricMod.java`。
- NeoForge 入口：`neoforge/src/main/java/com/meowconsole/neoforge/MeowConsoleNeoForgeMod.java`。
- 反矿透配置文件：`config/meowantixray.yml`。
- 已移除控制台、睡眠加速、玩家消息、飞行保护、Velocity、MCDR 兼容等非反矿透源码；保留本项目独立的 Modrinth 更新检测提示。

## Thread Boundary

- 后续 `MeowAnti-Xray` 相关任务单独开线程接管本目录。
- 本项目只维护反矿透配置、混入、区块包重写、reveals、性能统计和 Fabric/NeoForge 反矿透对齐。
- 控制台彩色、多人生睡觉、玩家消息、飞行保护、Velocity、MCDR、更新检查等任务回到 `E:\Code2026\fabricmod\meowconsole`。

## Verification

- 2026-04-27：`.\gradlew.bat :neoforge:test --console=plain` 通过。
- 2026-04-27：`.\gradlew.bat test :neoforge:test --console=plain` 通过。
- 2026-04-27：`.\gradlew.bat buildAllLoaders --console=plain` 通过。
- 2026-04-27：1.2.4 发布准备已清理 NeoForge `-test` 后缀，并给 Fabric 产物名加入 `fabric` 标志；`.\gradlew.bat clean buildAllLoaders --console=plain` 通过，产物包含 `meowantixray-fabric-1.2.4.jar` 与 `meowantixray-neoforge-1.2.4.jar`。
- 2026-04-27：修复 Fabric 爆炸 mixin 动态选择，`.\gradlew.bat runServer --console=plain` 可初始化到 `Meow Anti-Xray initialized`，随后因测试目录未同意 EULA 正常停止。
- 2026-04-27：NeoForge 端已升级到 `26.1.2.30-beta` / Minecraft `26.1.2` / Java `25`；`.\gradlew.bat :neoforge:runServer --console=plain` 成功启动到 `Done` 与 `Meow Anti-Xray startup complete`，随后手动停止测试进程。
- 2026-04-27：补测真实 NeoForge installer 流程：`neoforge-26.1.2.30-beta-installer.jar --install-server` 成功；复制 `meowantixray-neoforge-1.2.4.jar` 到安装服 `mods/` 后用 `run.bat nogui` 成功启动到 `Done` 与 `Meow Anti-Xray startup complete`。
- 2026-04-27：清理 VS Code 问题面板中的 JDT null/dead-code 诊断后，`.\gradlew.bat test --console=plain` 通过。
- 2026-04-27：新增独立 Modrinth 更新检测提示并补全中英更新日志后，`.\gradlew.bat test --console=plain` 通过。
- 2026-05-01：修复 `MeowServerCommands` 的 VS Code/JDT null type safety 告警后，`.\gradlew.bat compileJava` 与 `.\gradlew.bat :neoforge:compileJava` 通过。
- 2026-05-19：完成当前项目完成度盘点；`.\gradlew.bat test :neoforge:test --console=plain` 通过。
- 2026-05-19：按 PaperMC/Paper `main` HEAD `5c917dababca4087b9df4d44b415d85125479830` 对齐反矿透差异：补 Paper 默认隐藏/替换块、`bypass-permission` 默认节点、EntityBlock 不作为 mode2/3 假块、Paper update-radius 邻居集合测试，并保持 Fabric/NeoForge 权限桥为可选反射 + OP fallback；`.\gradlew.bat test :neoforge:test --console=plain` 与 `.\gradlew.bat buildAllLoaders --console=plain` 通过。
- 2026-05-19：完成 NeoForge 本地 spark 性能测试；安装 `spark-1.10.172-neoforge.jar` 到 `neoforge/run/mods`，开启本地 RCON 后运行 synthetic anti-xray rewrite 压测命令。真实 bot 连接未完成，原因是 `mineflayer/minecraft-protocol` 暂不支持 Minecraft `26.1.2`。可用 spark 报告：区块加载/forceload `https://spark.lucko.me/ExDlvACxiK`，短 profile `https://spark.lucko.me/gWGugZKjEy`，反矿透 synthetic rewrite 压测 `https://spark.lucko.me/MXVTdbfqAp`。最终验证 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。
- 2026-05-19：追加完成 `minecraft-protocol` 26.1.2 本地 shim 探测：npm 最新 `minecraft-data@3.110.2` 已知道 protocol `775`，但缺 `data/pc/26.1.2` 完整数据；临时复制/修补 1.21.11 数据只能通过 configuration，play 包结构/ID 会错位。随后用 raw TCP 协议 runner 按 NeoForge 源码 `GameProtocols.java` 抽取 26.1.2 packet ID，成功模拟离线玩家登录、回 keepalive、回 chunk batch 并触发真实 chunk 下发。正式 spark 跑图窗口：`SparkRunner1` 约 7.6s 收到 691 chunks / 38 batches，报告 `https://spark.lucko.me/wy4A3SM0jx`；metadata 显示 TPS 约 20、MSPT last1m mean 4.51ms / p95 7.78ms / max 45.94ms。该 raw runner 是临时压测工具，不是完整 mineflayer 支持。
- 2026-05-19：尝试改用 Carpet 假人进行 Fabric 真实区块加载压测；已在 `run/mods` 放入 `fabric-carpet-26.1-beta-1+v251217.jar`、`spark-1.10.172-fabric.jar` 与当前 Fabric 产物，并同意本地 `run/eula.txt`。但 `.\gradlew.bat runServer --console=plain` 在服务端启动前因 Carpet mixin 崩溃：`Player_scarpetEventsMixin#doInteract` 描述符缺少 26.1.2 的 `Vec3` 参数。结论：当前可用 Carpet beta 虽声明 `minecraft >26.0`，但运行期未对齐 Minecraft `26.1.2`，本轮无法用 Carpet 假人得到有效 spark 报告；残留 NeoForge dev server 进程已按当前项目路径限定清理，25565/25575 已释放。
- 2026-05-19：新增仓库内网络假玩家压测工具 `tools/mc-2612-load-runner.js`，直接实现 Minecraft `26.1.2` / protocol `775` 必需子集，绕开 Carpet 与 `mineflayer/minecraft-protocol` 版本缺口。NeoForge dev server 实测 4 个假网络玩家 + RCON 跑图 91s：连接 4/4、play 4/4、teleports 124、position packets 79、chunks 5103、batches 272、errors 0、chunks/s 56.07；spark 报告 `https://spark.lucko.me/xjeRM3esQF`。`.\gradlew.bat :neoforge:runServer --console=plain` 正常停止后 `BUILD SUCCESSFUL`，25565/25575 已释放。
- 2026-05-20：根据 PaperMC/Paper `main` HEAD `5c917da` 复核 anti-xray 异步处理：Paper 在原生 chunk packet 序列化生命周期中记录 buffer/palette/index 并提交轻量 `ChunkPacketInfoAntiXray` 到服务端 executor；本项目作为 Fabric/NeoForge 模组层实现需要 clone section/packet 快照，不能直接等价照搬，因此新增异步 rewrite 背压。配置新增 `async-queue-size`，默认 16；进入异步前先按 `async-worker-threads + async-queue-size` 获取 permit，超过容量直接同步 fallback，避免无界排队导致 heap OOM。最终 `.\gradlew.bat test :neoforge:test --console=plain` 通过。
- 2026-05-20：完成 NeoForge 8 假网络玩家 RCON spectator 跑图压测。修复前 8 人压测触发 `OutOfMemoryError: Java heap space`，栈落在 `FakeOreService$SectionSnapshot.stateAt`，根因是无界异步队列堆积大量快照任务。修复后 `async-queue-size=64` 跑 91s：38037 chunks、417.92 chunks/s、0 errors，spark `https://spark.lucko.me/FyVDZTMgHb`，heap used 6314MB，TPS last1m 19.31，MSPT last1m mean 29.93ms / p95 229.12ms。收紧为 `async-queue-size=16` 后 91s：36233 chunks、398.06 chunks/s、0 errors，spark `https://spark.lucko.me/yFi0jlgOf4`，heap used 4840MB，TPS last1m 17.39，MSPT last1m mean 45.93ms / p95 309.05ms。结论：16 是更保守的防 OOM 默认，生产可按内存把队列调到 32/64 换吞吐。
- 2026-05-20：发布准备版本号按用户要求从语义化首版 `1.0.0` 开始，重写 `CHANGELOG.md` 为面向使用者的中英文标准更新日志，并优化 `MOD_INTRO_ZH.md` / `MOD_INTRO_EN.md`。新增 Gradle 任务 `releaseAllLoaders`，会构建 Fabric/NeoForge 并把两个主 jar 复制到统一目录 `build/release/`。执行 `.\gradlew.bat clean releaseAllLoaders --console=plain` 通过，产物为 `build/release/meowantixray-fabric-1.0.0.jar` 与 `build/release/meowantixray-neoforge-1.0.0.jar`，已检查内嵌 Fabric/NeoForge metadata 版本均为 `1.0.0`。
- 2026-05-20：按用户要求将更新日志按版本号归档到独立目录 `changelogs/`：`changelogs/1.0.0.zh-CN.md` 与 `changelogs/1.0.0.en-US.md`；根目录 `CHANGELOG.md` 仅保留版本索引链接。
- 2026-05-22：按 Modrinth 地址 `https://modrinth.com/project/meowanti-xray` 完善独立更新提示：服务端启动后异步检查 Modrinth compatible version，发现新版本时向在线管理员/OP 发送固定格式消息（当前版本 -> 新版本、简短更新内容、下载直链、版本页），管理员上线时也会补发当前新版本提示；`/antixray status` 玩家执行时复用可点击格式。`.\gradlew.bat test :neoforge:test --console=plain` 与 `.\gradlew.bat buildAllLoaders --console=plain` 通过。
- 2026-05-22：为真实联机测试启动本地双端 dev server。Fabric 监听 `127.0.0.1:25565`，RCON `25575`；NeoForge 监听 `127.0.0.1:25566`，RCON `25576`。为避免测试到旧 jar 或 Carpet 26.1.2 崩溃问题，已把 `run/mods` 与 `neoforge/run/mods` 中原有 jar 临时移动到各自的 `disabled-for-local-test/` 子目录；当前 dev server 均加载项目源码版本 `meowantixray 1.0.0`。
- 2026-05-22：完成 Fabric 真实联机 spark 跑图采样，Fabric 监听 `127.0.0.1:25565`，报告 `https://spark.lucko.me/btQdhJh1gH`。采样 2m01s / 2400 ticks / 1 player：TPS 稳定 20.00，MSPT median 3.31ms / p95 19.7ms / max 94.6ms，server thread 主要时间在 `MinecraftServer.waitUntilNextTick()` idle；`meowantixray` 在 mods 视图占 server thread 3.55%，轻量单人跑图下开销可接受。过程中修复 Fabric 登录时权限 lambda 反射 `IllegalAccessException`：`MinecraftCompat.invokeSingleArg` 与双参数 overload 调用前执行 `method.setAccessible(true)`；`.\gradlew.bat compileJava --console=plain` 通过。
- 2026-05-22：按用户要求关闭本地 Fabric / NeoForge 测试服，RCON 返回 `Stopping the server` 后 25565/25566/25575/25576 已释放；同时更新 `changelogs/1.0.0.zh-CN.md` 与 `changelogs/1.0.0.en-US.md`，发布说明更突出独立项目的性能优势，并明确 MeowConsole 新版本将不再维护内置反矿透，后续反矿透功能由 Meow Anti-Xray 承接。
- 2026-05-22：修复 `ModrinthUpdateChecker.literal` 处 VS Code/JDT null type safety 黄色告警：为 Fabric/NeoForge 编译都加入 `org.eclipse.jdt:org.eclipse.jdt.annotation` 的 `compileOnly` 依赖，并将 `requireText` 标注为 JDT `@NonNull String`，避免 suppression 引出新的 JDT 提示；`.\gradlew.bat compileJava :neoforge:compileJava --console=plain` 通过。
- 2026-05-22：接入用户提供的 Modrinth API token 完成项目线上维护和首版发布。项目 ID `8pl8obwY`，slug `meowanti-xray`，标题 `Meow Anti-Xray`，状态仍为 `draft` / `requested_status=approved` 等待 Modrinth 审核；已填写中英文项目正文、分类 `optimization`/`utility`、附加分类 `game-mechanics`/`management`、GitHub source/issues/wiki，客户端 `unsupported`、服务端 `required`。执行 `.\gradlew.bat clean releaseAllLoaders --console=plain` 通过，并上传版本 `1.0.0`（version id `misc3vI5`，release/listed/featured，MC `26.1.2`，loaders `fabric` + `neoforge`），文件为 `meowantixray-fabric-1.0.0.jar` 与 `meowantixray-neoforge-1.0.0.jar`。
- 2026-05-22：用户指出 Modrinth 合并版本展示会误导为单文件同时兼容 Fabric/NeoForge；本地已修正更新检测版本比较，忽略 SemVer build metadata（如 `1.0.0+fabric` / `1.0.0+neoforge`）并新增 `ModrinthUpdateCheckerTest`，`.\gradlew.bat test :neoforge:test releaseAllLoaders --console=plain` 通过。Modrinth 已改为两个独立版本：`1.0.0+fabric`（version id `yeuyLiT2`，只挂 `meowantixray-fabric-1.0.0.jar` 与 Fabric loader）和 `1.0.0+neoforge`（version id `8lfSaQK0`，只挂 `meowantixray-neoforge-1.0.0.jar` 与 NeoForge loader）；旧合并版本 `misc3vI5` 已删除。上传时最初遇到 Modrinth/S3 临时错误，改用禁用环境代理的 requests 重试后成功。
- 2026-05-22：修正 Modrinth 版本兼容范围：Fabric 与 NeoForge 两个线上版本均标记 Minecraft `26.1`、`26.1.1`、`26.1.2`。本地 NeoForge metadata 原先为 `[26.1.2,26.1.3)`，已放宽为 `[26.1,26.1.3)`，`.\gradlew.bat releaseAllLoaders --console=plain` 通过，并确认 NeoForge jar 内 `neoforge.mods.toml` 的 minecraft `versionRange = "[26.1,26.1.3)"`。已删除旧 NeoForge version id `8lfSaQK0` 并重传新 `1.0.0+neoforge`，新 version id `WHv5uBDD`；Fabric version id 仍为 `yeuyLiT2`。
- 2026-06-01：校准 Modrinth 公开状态：项目 `8pl8obwY` / slug `meowanti-xray` 已于 2026-05-31 审核通过，当前状态 `approved`，线上版本仍为 Fabric `yeuyLiT2` 与 NeoForge `WHv5uBDD`，公开页 `https://modrinth.com/mod/meowanti-xray`。
- 2026-06-01：启动 1.0.1 稳定维护准备：增强 `/antixray status` 输出 loader / mod version / Minecraft version；增强 `/antixray profile` 输出 async 容量、in-flight、available permits 与同步 fallback 计数，方便服主判断是否触发异步背压；忽略本地 `.playwright-cli/` 测试缓存。验证 `.\gradlew.bat test :neoforge:test --console=plain` 与 `.\gradlew.bat buildAllLoaders --console=plain` 通过。
- 2026-06-01：完成 1.0.1 发布准备：`mod_version` 已升到 `1.0.1`，新增 `changelogs/1.0.1.zh-CN.md` 与 `changelogs/1.0.1.en-US.md`，根目录 `CHANGELOG.md` 已加入 1.0.1 索引；再次验证 `.\gradlew.bat buildAllLoaders --console=plain` 通过。
- 2026-06-01：开始性能路径瘦身，目标是不改效果前提下减少热路径分配与重复复制。已把 `SectionSnapshot` 从全量 `states[]` 改为按需从 palette/unpacked 还原，去掉 `prepareChunkRewrite` 到 async task 的一层重复 clone，并为 `remapDensePaletteIndices` 保留兼容重载。验证 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。
- 2026-06-01：补充洞穴可见性回归测试：合成 section 验证连通到边界的洞穴空气旁隐藏矿石仍保持 exposed、不被回填；完全包裹矿石仍为 unexposed、可回填。验证 `.\gradlew.bat test :neoforge:test buildAllLoaders --console=plain` 通过。
- 2026-06-01：完成 1.0.1 发布到 Modrinth：本地执行 `.\gradlew.bat test :neoforge:test releaseAllLoaders --console=plain` 通过，产物为 `build/release/meowantixray-fabric-1.0.1.jar` 与 `build/release/meowantixray-neoforge-1.0.1.jar`。线上项目简介已更新为新版中英文表情版简介并包含压测验证表格；新增独立版本 `1.0.1+fabric`（version id `39vD2jaz`，文件 `meowantixray-fabric-1.0.1.jar`，依赖 Fabric API）和 `1.0.1+neoforge`（version id `gOiixdr9`，文件 `meowantixray-neoforge-1.0.1.jar`），均为 `listed` / `featured`，MC 兼容 `26.1`、`26.1.1`、`26.1.2`。
